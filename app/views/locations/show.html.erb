

<table class="table">
  <tr>
    <td><strong>Address1:</strong> <%= @location.address1 %></td>
    <td colspan=2> <strong>Address2:</strong> <%= @location.address2 %></td>
  </tr>
  <tr>
    <td><strong>City:</strong> <%= @location.city %></td>
    <td><strong>County:</strong> <%= @location.county %></td>
    <td><strong>Country:</strong> <%= @location.country %></td>
  </tr>
</table>


<%= link_to 'Edit', edit_location_path(@location) %> |
<%= link_to 'Back', locations_path %>

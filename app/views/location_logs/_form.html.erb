<%= form_for(@location_log) do |f| %>
  <% if @location_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location_log.errors.count, "error") %> prohibited this location_log from being saved:</h2>

      <ul>
      <% @location_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :latitude %><br>
    <%= f.text_field :latitude %>
  </div>
  <div class="field">
    <%= f.label :longitude %><br>
    <%= f.text_field :longitude %>
  </div>
  <div class="field">
    <%= f.label :position_time %><br>
    <%= f.datetime_select :position_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
